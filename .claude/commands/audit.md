---
description: Read-only drift audit of code vs docs
---
Do a read-only audit of drift between the code and `docs/` (data model, event model, visualization spec, architecture). List anything in code that the docs don't describe and anything in the docs for completed phases that the code lacks. Write findings to `docs/drift-report.md` and summarise. Change nothing else.
